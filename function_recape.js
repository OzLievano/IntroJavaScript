// functions package up code to use and reuse blocks of code
// Parameters are variables that are used to store the data
// Arguments are the actual data that's passed into a function when invoked/called

.// x and y are parementers in this function declar

function add(x,y) {
  //function body
  var sum = x + y;
  return sum; // return statement
}

// 1 and 2 are passed into the function as Arguments
console.log(add(1,2));
